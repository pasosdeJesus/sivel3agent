Modify the scraper to **always store the full text** of every article/report, then classify it for relevance to DDHH/DIH/VPS in Colombia or Palestine. This ensures no information is lost, and classification can be revisited later if criteria change.

**Why this is needed:**
- Initial tests (#6) generated pre‑alerts from irrelevant articles (cultural events, infrastructure, global news).
- RSS feeds now give full text via `content:encoded` (WordPress blogs) or `description` — we can classify based on the complete article, not just the excerpt.
- High‑value sources (Comisión Intereclesial, Verdad Abierta, Prensa Rural) are now included and provide dense DDHH content.

**Core principle:** Store first, classify later. Full text is always saved. Classification happens after storage, using the complete text. Only articles with `is_relevant = true` proceed to #6 pre‑alert extraction.

**Historical coverage:** This issue handles classification of current RSS content. Historical crawling (to cover back to 2025‑07) is addressed in #13.

---

## Current Feeds (Updated from #9)

RSS feeds with full‑text support (WordPress/SPIP `content:encoded`):

| # | Source | RSS URL | Region | Full text? |
|---|--------|---------|--------|------------|
| 3 | INDEPAZ | `indepaz.org.co/feed/` | Colombia | ✅ `content:encoded` |
| 4b | El Tiempo | `eltiempo.com/rss/justicia_conflicto-y-narcotrafico.xml` | Colombia | ⚠️ `description` only |
| 5 | Comisión Intereclesial | `justiciaypazcolombia.com/feed/` | Colombia | ✅ `content:encoded` |
| 6 | MiPutumayo Noticias | `miputumayo.com.co/feed/` | Putumayo | ✅ `content:encoded` |
| 7 | ReliefWeb oPt | `reliefweb.int/updates/rss.xml?search=…` | Palestine | ✅ `description` (full) |
| 8 | HRW | `hrw.org/rss/news` | Colombia | ✅ `description` (full) |
| 13 | Verdad Abierta | `verdadabierta.com/feed/` | Colombia | ✅ `content:encoded` |
| 14 | Prensa Rural | `prensarural.org/spip/spip.php?page=backend` | Colombia | ✅ `content:encoded` |

---

## Integration with Existing Issues

| Issue | Relation |
|-------|----------|
| **#4 / #9** (Scraper) | This modifies the scraper logic to store full text and classify after storage. |
| **#6** (LLM extraction) | Only articles classified as `is_relevant = true` proceed to JSON extraction. |
| **#13** (Historical crawler) | Covers back to 2025‑07 for sources with paginated archives. |

---

## Data Flow

```
1. RSS fetch (full text from content:encoded)
2. Store in source (is_relevant = NULL)
3. Fast keyword classifier
   ├─ high confidence + not relevant → is_relevant = false (skip LLM)
   ├─ high confidence + relevant → is_relevant = true (skip LLM)
   └─ low confidence / ambiguous → LLM classifier
4. Update is_relevant + classification_reason
5. #6 extract-and-save.ts queries WHERE is_relevant = true
```

**Key principle:** Full text is always stored. Classification can be re‑run later if needed.

---

## Source Table Schema

Add classification columns via migration:

```sql
ALTER TABLE source ADD COLUMN is_relevant BOOLEAN DEFAULT NULL;
ALTER TABLE source ADD COLUMN classification_reason TEXT;

CREATE INDEX idx_source_is_relevant ON source(is_relevant);
CREATE INDEX idx_source_medium ON source(medium);
```

---

## Implementation

### 1. Migration

Create `db/migrations/XXXXXX_add_classification_columns.ts`:

```typescript
import { Kysely } from 'kysely'

export async function up(db: Kysely<any>): Promise<void> {
  await db.schema
    .alterTable('source')
    .addColumn('is_relevant', 'boolean', (col) => col.defaultTo(null))
    .execute()

  await db.schema
    .alterTable('source')
    .addColumn('classification_reason', 'text')
    .execute()

  await db.schema
    .createIndex('idx_source_is_relevant')
    .on('source')
    .column('is_relevant')
    .execute()

  await db.schema
    .createIndex('idx_source_medium')
    .on('source')
    .column('medium')
    .execute()
}

export async function down(db: Kysely<any>): Promise<void> {
  await db.schema.dropIndex('idx_source_medium').execute()
  await db.schema.dropIndex('idx_source_is_relevant').execute()
  await db.schema.alterTable('source').dropColumn('classification_reason').execute()
  await db.schema.alterTable('source').dropColumn('is_relevant').execute()
}
```

### 2. Fast Classifier (Keywords)

Create `lib/classifyByKeywords.ts`. Two‑stage logic:

1. Must match ≥1 violence keyword AND ≥1 region keyword
2. Confidence: `high` if clearly matches both, `low` if only matches one

```typescript
// lib/classifyByKeywords.ts

const VIOLENCE_KEYWORDS = [
  // Español
  'asesinato', 'masacre', 'desplazamiento', 'amenaza', 'ejecución',
  'tortura', 'desaparición', 'violencia', 'conflicto', 'guerrilla',
  'paramilitar', 'frente', 'ataque', 'homicidio', 'secuestro',
  'despojo', 'reclutamiento', 'lider social', 'defensor',
  'derechos humanos', 'DDHH', 'DIH', 'violación',
  'masacr', 'asesin', 'muert', 'combate', 'enfrentamiento',
  'asesinad', 'bombardeo', 'minas antipersona', 'artefacto explosivo',
  // English
  'kill', 'murder', 'massacre', 'displacement', 'forced', 'settler',
  'occupation', 'detention', 'torture', 'human rights',
  'extrajudicial', 'armed group', 'ceasefire', 'attack',
  'civilian', 'casualt', 'genocide', 'apartheid', 'war crime',
]

const COLOMBIA_KEYWORDS = [
  'Putumayo', 'Mocoa', 'Puerto Asís', 'Colombia', 'Cauca', 'Nariño',
  'Villagarzón', 'Orito', 'Sibundoy', 'zona de reserva campesina',
  'Antioquia', 'Chocó', 'Valle del Cauca', 'Norte de Santander',
  'Arauca', 'Catatumbo', 'Bajo Cauca', 'Magdalena Medio',
  'colombian', 'Bogotá', 'Medellín', 'Cali',
]

const PALESTINE_KEYWORDS = [
  'Palestine', 'Gaza', 'West Bank', 'Israel', 'occupied Palestinian',
  'Jerusalem', 'Ramallah', 'Hebron', 'Nablus', 'Jenin', 'Rafah',
  'Palestin', 'Gazan', 'Israeli', 'settler violence',
  'oPt', 'occupied territory',
]

export function classifyByKeywords(
  text: string,
  title: string,
): { relevant: boolean; confidence: 'high' | 'low'; reason: string } {
  const textLower = text.toLowerCase()
  const titleLower = title.toLowerCase()

  const hasViolence = VIOLENCE_KEYWORDS.some(
    (k) => textLower.includes(k) || titleLower.includes(k),
  )
  const hasColombia = COLOMBIA_KEYWORDS.some(
    (k) => textLower.includes(k) || titleLower.includes(k),
  )
  const hasPalestine = PALESTINE_KEYWORDS.some(
    (k) => textLower.includes(k) || titleLower.includes(k),
  )

  if (hasViolence && (hasColombia || hasPalestine)) {
    const region = hasColombia ? 'Colombia' : 'Palestine'
    return { relevant: true, confidence: 'high', reason: `violence+${region}` }
  }

  if (hasViolence && !hasColombia && !hasPalestine) {
    return {
      relevant: false,
      confidence: 'low',
      reason: 'violence outside scope',
    }
  }

  if (!hasViolence && (hasColombia || hasPalestine)) {
    return {
      relevant: false,
      confidence: 'low',
      reason: 'region match but no violence keywords',
    }
  }

  return { relevant: false, confidence: 'high', reason: 'no match' }
}
```

### 3. LLM Classifier

Create `lib/classifyNewsLLM.ts`. Reuses `callLLM` pattern from `extractPreAlert.ts`.

```typescript
// lib/classifyNewsLLM.ts

const LLM_URL = process.env.LLM_URL || 'http://localhost:11434/api/generate'
const LLM_MODEL = process.env.LLM_MODEL || 'qwen2.5:7b-instruct-q4_K_M'

export async function classifyNewsLLM(
  text: string,
  title: string,
): Promise<{ relevant: boolean; reason: string }> {
  const prompt = `
Eres un clasificador de noticias para documentación de violencia política en Colombia y Palestina.

Tarea: Lee TODO el texto y determina si describe un caso de:
- Violación de Derechos Humanos (DDHH)
- Infracción al Derecho Internacional Humanitario (DIH)
- Violencia Político-Social (VPS)

Criterios de inclusión:
- Asesinatos, desapariciones, tortura, ejecuciones extrajudiciales
- Desplazamiento forzado, masacres, amenazas a líderes sociales
- Ataques a comunidades, restricciones a la libertad de movimiento
- Conflictos armados, presencia de grupos armados ilegales
- Represión política, persecución a defensores de DDHH
- Violencia sexual en contexto de conflicto
- Reclutamiento forzado de menores

Región: Solo Colombia o Palestina. Si el artículo es de otro país, responde NO.

Noticia:
Título: ${title}
Texto: ${text.slice(0, 8000)}

Responde EXACTAMENTE en este formato:
RELEVANTE: [SÍ/NO]
RAZÓN: [Una frase breve]
`

  const response = await fetch(LLM_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      model: LLM_MODEL,
      prompt,
      stream: false,
      options: { temperature: 0.1, top_p: 0.9 },
    }),
  })

  if (!response.ok) {
    throw new Error(`Ollama API error: ${response.status}`)
  }

  const data = (await response.json()) as { response: string }
  const relevantMatch = data.response.match(/RELEVANTE:\s*(SÍ|NO)/i)
  const reasonMatch = data.response.match(/RAZÓN:\s*(.+)/i)

  return {
    relevant: (relevantMatch?.[1]?.toUpperCase() === 'SÍ'),
    reason: reasonMatch?.[1]?.trim() || 'parse error',
  }
}
```

**Note:** Text is truncated to 8000 chars for the LLM classifier prompt to stay within reasonable token limits. The full text is always stored in `clean_text` regardless.

### 4. Modified Scraper Flow

Modify `scripts/scrape-news.ts`:

```
For each RSS item:
  1. Extract full text from content:encoded or fallback to description
  2. Clean HTML → clean_text
  3. Store in source table (is_relevant = NULL)
  4. Run keyword classifier → if high confidence, set is_relevant directly
  5. If low confidence, run LLM classifier → set is_relevant
  6. Log: 📥 Stored → 🔍 Keyword:relevant → 🤖 LLM:relevant (or skipped)
```

Key changes to `fetchRSSFeed`:
- Use `item['content:encoded']` or `item.content` (rss-parser exposes `content:encoded` as `content` for WordPress feeds) for full text
- For SPIP (Prensa Rural), `rss-parser` maps `content:encoded` to `content` field

### 5. Update extract-and-save.ts

```typescript
// Only process relevant sources
const sources = await db
  .selectFrom('source')
  .select(['id', 'url', 'title', 'published_at', 'clean_text', 'medium'])
  .leftJoin('pre_alert_source', 'source.id', 'pre_alert_source.source_id')
  .where('pre_alert_source.pre_alert_id', 'is', null)
  .where('clean_text', 'is not', null)
  .where('is_relevant', '=', true)    // ⬅️ Only relevant articles
  .limit(10)
  .execute()
```

### 6. Classification Script (Standalone)

Create `scripts/classify-news.ts` for re‑classification (useful if criteria change):

```typescript
import { newKyselyPostgresql } from '../.config/kysely.config.js'
import { classifyByKeywords } from '../lib/classifyByKeywords'
import { classifyNewsLLM } from '../lib/classifyNewsLLM'

async function classifyStoredSources(reset = false) {
  const db = newKyselyPostgresql()

  const sources = await db
    .selectFrom('source')
    .select(['id', 'title', 'clean_text', 'is_relevant'])
    .where((eb) => reset ? eb('id', '>', 0) : eb('is_relevant', 'is', null))
    .where('clean_text', 'is not', null)
    .limit(50)
    .execute()

  for (const source of sources) {
    const kw = classifyByKeywords(source.clean_text || '', source.title || '')

    let isRelevant: boolean
    let reason: string

    if (kw.confidence === 'high') {
      isRelevant = kw.relevant
      reason = `[kw] ${kw.reason}`
    } else {
      try {
        const llm = await classifyNewsLLM(source.clean_text || '', source.title || '')
        isRelevant = llm.relevant
        reason = `[llm] ${llm.reason}`
      } catch {
        isRelevant = kw.relevant
        reason = `[kw-fallback] ${kw.reason}`
      }
    }

    await db
      .updateTable('source')
      .set({ is_relevant: isRelevant, classification_reason: reason })
      .where('id', '=', source.id)
      .execute()

    const icon = isRelevant ? '✅' : '❌'
    console.log(`${icon} ${source.title?.slice(0, 80)} → ${reason}`)
  }
}

const reset = process.argv.includes('--reset')
classifyStoredSources(reset)
  .then(() => process.exit(0))
  .catch((e) => { console.error(e); process.exit(1) })
```

---

## Acceptance Criteria

- [x] Migration adds `is_relevant` and `classification_reason` columns to `source` table
- [x] Scraper stores full text from `content:encoded` for all WordPress/SPIP feeds
- [x] Keyword classifier correctly identifies clearly relevant/irrelevant articles
- [x] LLM classifier correctly resolves ambiguous cases
- [x] `is_relevant` is set to `true` or `false` for every article after scrape
- [x] `extract-and-save.ts` only processes `is_relevant = true` articles
- [x] Classification can be re‑run via `scripts/classify-news.ts --reset`
- [x] At least 10 relevant articles are detected across all sources in a single run (36/100)
- [x] Logs clearly show: stored → classified → result for each article

---

## Dependencies

- Requires #4 / #9 (scraper) to be functional with current feeds
- Requires #6 (LLM client / `extractPreAlert.ts` pattern)
- Requires Ollama with `qwen2.5:7b-instruct-q4_K_M`

---

## Related Issues

- Epic: [#36](https://github.com/pasosdeJesus/sivel3/issues/36)
- Predecessors: #4, #6, #9
- Successor: #13 (historical crawler for 2025‑07+ coverage)
- Related: #2 (source table schema)

---

## Notes

- **RSS text completeness:** WordPress feeds include full article in `content:encoded` (rss‑parser maps to `item.content`). SPIP feeds use the same field. No web scraping needed for these sources.
- **No truncation for storage:** Full `clean_text` is always stored. Truncation (8000 chars) only applies to LLM prompt input.
- **Fallback:** If LLM classifier fails, fallback to keyword classifier result and mark as `classification_reason = '[kw-fallback] …'`.
- **Re‑classification safe:** Run `scripts/classify-news.ts --reset` to re‑run classification on all sources if keywords or criteria change.

---

> *"Whatever you do, work at it with all your heart, as working for the Lord, not for human masters."* (Colossians 3:23)
