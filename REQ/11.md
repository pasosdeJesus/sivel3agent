# REQ/11 — Fine‑Tuning for Noche y Niebla Classification

Fine‑tune the best candidate model to improve structured JSON extraction from news articles, with focus on the two weakest metrics from #10 evaluation:
1. **Aggression code classification** (currently 71% for both top models)
2. **Group identification** (currently 0‑14%)

**Decision:** Postponed for MVP. See rationale below.

---

## Evaluation Context (#10)

| Model | Score | Aggression | Groups | Speed | VRAM |
|-------|-------|------------|--------|-------|------|
| qwen3:8b | 73.1% | 71% | 0% | 23.3s | 5.2 GB |
| qwen2.5:7b | 73.0% | 71% | 0% | 7.0s | 4.7 GB |
| qwen2.5:14b (coder/instruct) | 60.7% | 43% | 0% | 15.5s | 9.0 GB |

**Key finding:** 14B does NOT outperform 7B/8B. The true Qwen2.5-14B-Instruct GGUF is identical to the Coder variant — Qwen didn't release a separate Instruct 14B. Our 14B evaluation is definitive.

---

## Why Postponed

1. **No 14B advantage.** The larger model performs worse (43% vs 71%). Fine‑tuning a 14B that underperforms 7B is not worth the investment.

2. **Dataset bottleneck.** Fine‑tuning requires 100‑200 annotated examples. We only have 7. The [golden dataset expansion](#dataset-strategy) needs to happen first.

3. **Better ROI alternatives exist.** Before investing 100+ hours in annotation + training:
   - vLLM with guided JSON (#14) would force schema‑compliant output
   - Better source scraping (#13) would provide cleaner input text
   - The SKILL prompt already produces valid JSON 100% of the time

4. **ROCm compatibility risk.** Unsloth has limited AMD support. Most LoRA/QLoRA tutorials assume CUDA. We'd likely need Google Colab (T4, free tier) as fallback.

5. **71% is adequate for MVP.** The current model correctly classifies 5/7 aggression types and produces valid JSON every time.

---

## Dataset Strategy

When we have enough annotated cases (target: 100‑200), the process is:

### Source

Cases from the Banco de Datos with verifiable press sources, same methodology as the 7 Putumayo cases:

1. Obtain XML export from CINEP/PPP (like `casos-put-2025-I.xrlat`)
2. Convert to JSON via `scripts/convert-xrlat.ts` (#15)
3. Match each case to its press source URL
4. Scrape the full article text via `scripts/scrape-golden.ts`
5. The Banco de Datos annotation becomes the ground truth `output`

### Semi‑automated annotation

1. Run the best current model (qwen3:8b) on each article to generate a draft JSON
2. Human reviewer corrects the draft (faster than annotating from scratch)
3. 20% double‑annotated for quality control

### Target diversity

- **Regions:** Putumayo, Cauca, Antioquia, Nariño, Bogotá, Palestina
- **Types:** DD.HH. (A), VPS (B), DIHC (D), Acción Bélica (C)
- **Periods:** 2020‑2026, focus on 2025‑2026
- **Sources:** Mix of national press, local media, NGO reports

---

## Fine‑Tuning Plan (When Ready)

### Candidate Model

**qwen2.5:7b-instruct** — best speed/precision balance (7.0s, 71%). qwen3:8b is 3× slower for same precision.

### Technique: QLoRA with Unsloth

| Parameter | Value |
|-----------|-------|
| Rank (r) | 16 |
| Alpha | 32 |
| Dropout | 0.1 |
| Learning rate | 2e‑4 |
| Batch size | 4 |
| Epochs | 3 |
| Quantization | 4‑bit (QLoRA) |
| Max seq length | 2048 |

### Environment

| Option | Feasibility |
|--------|-------------|
| Local RX 9060 XT | ⚠️ Unsloth ROCm support uncertain |
| Google Colab (T4) | ✅ Free, 15 GB VRAM, CUDA |
| TuneKit | ✅ Generates Colab notebook |

**Recommended:** Google Colab with T4 GPU (free tier). Upload dataset, run Unsloth, download LoRA adapter.

### Expected improvement

| Metric | Current | Target |
|--------|---------|--------|
| Aggression accuracy | 71% | >80% |
| Group identification | 0% | >40% |
| JSON validity | 100% | 100% |
| Speed | 7.0s | ~7.0s (LoRA adds negligible overhead) |

**Realistic target:** +10‑15% on aggression, +30‑40% on groups. The 71%→85% jump claimed in earlier versions is optimistic for a 7B model.

---

## Acceptance Criteria

- [ ] 100‑200 annotated examples in JSONL format (`data/finetune-dataset.jsonl`)
- [ ] Fine‑tuning runs without errors (local or Colab)
- [ ] Fine‑tuned model shows ≥10% improvement in aggression accuracy
- [ ] Fine‑tuned model shows ≥30% improvement in group identification
- [ ] JSON output remains 100% valid against `relato.schema.json`
- [ ] LoRA adapter saved and documented
- [ ] Process documented in `doc/fine-tuning.md`

---

## Dependencies

- Requires expanded golden dataset (100‑200 cases with press sources)
- Requires #10 (evaluation framework to measure improvement)
- Requires #15 (XML→JSON converter for CINEP data)
- Requires `unsloth` + `trl` (Python) or Google Colab access
- **Blocks:** Nothing. Postponed for MVP.

---

## Related Issues

- Epic: [#36](https://github.com/pasosdeJesus/sivel3/issues/36)
- Predecessor: #10 (model evaluation)
- Related: #6 (LLM extraction), #15 (XML↔JSON converters)
- Alternative paths: #14 (vLLM guided JSON), #13 (historical crawler)

---

> *"The heart of the discerning acquires knowledge, for the ears of the wise seek it out."* (Proverbs 18:15)
